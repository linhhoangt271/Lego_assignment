# Setup

Install dependencies and open the consolidated notebook:

```bash
pip install -r requirements.txt
jupyter notebook Models_Summary.ipynb
```

Place these local-only files at the repository root before running training cells:

```text
minifigs.json
images/
```

They are intentionally ignored by Git.

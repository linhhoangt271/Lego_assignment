# Quick Start

Use the consolidated notebook:

```bash
cd big_data_assignment_2
pip install -r requirements.txt
jupyter notebook Models_Summary.ipynb
```

Before running training cells, place the local dataset at the repository root:

```text
minifigs.json
images/
```

Both are ignored by Git. Model checkpoints and generated results are also ignored, so only notebook and documentation changes should be committed.
